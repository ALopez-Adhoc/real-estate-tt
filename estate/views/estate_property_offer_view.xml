<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="estate_property_offer_view_tree" model="ir.ui.view">
        <field name="name">Offer</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <tree editable="bottom" decoration-success="status=='accepted'" decoration-danger="status=='refused'" >
                <field name="price" />
                <field name="partner_id" />
                <field name="validity" />
                <field name="date_deadline" />
                <field name="property_type_id" />
                <field name="status" invisible="1"/>
                <button name="accept_offer" title="Accept" icon="fa-check" type="object" attrs="{'invisible': ['|',('status', '=', 'accepted'), ('status', '=', 'refused')]}"/>
                <button name="cancel_offer" title='Refuse' icon="fa-times" type="object" attrs="{'invisible': ['|',('status', '=', 'accepted'), ('status', '=', 'refused')]}"/>
            </tree>
        </field>
    </record>

    <record id="estate_property_offer_view_form" model="ir.ui.view">
        <field name="name">Offer</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="price" />
                        <field name="partner_id" />
                        <field name="validity" />
                        <field name="date_deadline" />
                        <field name="status" />
                    </group>
                </sheet>
            </form>
        </field>
    </record>

</odoo>
